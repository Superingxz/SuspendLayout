<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.morligy.simple">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".stickynavsimple.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity  android:name=".stickynavsimple.activity.SimpleStickActivity"
            android:label="SimpleStickActivity"
            />
        <activity
            android:name=".stickynavsimple.activity.PullToRefreshStickActivity"
            android:label="PullToRefreshStickActivity" />
        <activity android:name=".stickynavsimple.activity.TopViewOverOneScreenActivity"/>
        <activity android:name=".stickynavsimple.activity.SwipeRefreshLayoutActivity"/>
        <activity android:name=".stickynavsimple.activity.TopOperateActivity"/>
    </application>

</manifest>